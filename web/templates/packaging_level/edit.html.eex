<h2>Edit packaging level</h2>

<%= render "form.html", changeset: @changeset,
                        action: packaging_level_path(@conn, :update, @packaging_level) %>

<%= link "Back", to: packaging_level_path(@conn, :index) %>
