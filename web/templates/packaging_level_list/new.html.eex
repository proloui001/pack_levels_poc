<h2>New packaging level list</h2>

<%= render "form.html", changeset: @changeset,
                        action: packaging_level_list_path(@conn, :create) %>

<%= link "Back", to: packaging_level_list_path(@conn, :index) %>
